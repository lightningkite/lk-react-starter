import {Container} from "@mui/material"
import PageHeader from "components/PageHeader"
import React, {FC} from "react"

export const AnotherPage: FC = () => {
  return (
    <Container>
      <PageHeader title="Another Page" />
    </Container>
  )
}
